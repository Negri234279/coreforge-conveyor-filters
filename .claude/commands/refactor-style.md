Read src/styles/design-system.md then refactor $ARGUMENTS to apply the 
established design system.

Keep the existing logic and structure intact — only update visual styles:
- Replace generic tailwind classes with the project's card/modal patterns
- Apply the color tokens and border styles from the design system
- Add hover/focus states consistent with the home page
- Match the industrial dark aesthetic